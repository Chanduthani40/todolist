<%- include('header'); -%>
    <div class="box" id="heading">
        <h1><%= okkaticheppana %></h1>
    </div>
    <div class="box" >
            <% newlist.forEach(function(list) { %>
                <form action="/delete" method="post" >
                    <div class="item">
                        <input type="checkbox" name="checkbox" value="<%= list._id %>" onchange="this.form.submit()">
                        <p> <%= list.name %></p>
                    </div>
                    <input type="hidden" name="geeparams"  value=<%= okkaticheppana %> ></input>
                </form>
            <% } ) %>
            <form action="/" method="post" class="item">
                <input name="list" type="text" id="demo" placeholder="New Item" autocomplete="off">
                <button onclick="return myfunction()" type="submit" name="list1" value=<%= okkaticheppana %> > + </button>
            </form>
           
    </div>
    
    <%- include('footer'); -%>